# Project Rules

## Git Workflow

- タスク開始時は必ず新しいブランチを作成すること
  - ブランチ名の例: `feat/add-new-feature`, `fix/fix-bug`, `docs/update-documentation`
- タスク完了後は必ず以下を順番に実行すること：
  1. `git commit`
  2. `git push`
  3. Pull Request を作成
